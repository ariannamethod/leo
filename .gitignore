# build artifacts
leo
leo.asan
tests/test_leo
*.o
*.dSYM/

# runtime state (regenerated from corpus + dialogue)
*.state
leo_mycelium/

# AML bridge build artifacts (source is vendored; the .a/.o are built, not committed)
ariannamethod/*.a
ariannamethod/*.o

# Local Fable audit reports — never distributed
AUDIT_FABLE_*.md
