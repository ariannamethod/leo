# Leo — Language Engine Organism
# Core dependencies for Leo's async field-based architecture

# NumPy: vectorized operations, float64 precision, linear regression
# Used in:
# - distribution_entropy (leo.py, neoleo.py): Shannon entropy calculations
# - gowiththeflow.py: linear regression for theme trajectory slopes
# - mathbrain.py: quality coordination calculations
# - All modules: precise floating point operations
numpy>=1.20.0

# SentencePiece: subword tokenization for parallel voice
# Used in:
# - subword.py: SubwordField builds bigram/trigram on subwords (not chars!)
# - Parallel voice alongside character trigrams
# - Enriches Leo's speech with word-level patterns
sentencepiece>=0.1.99

# Async support (Python 3.7+ stdlib, but explicit for clarity)
# Used in:
# - Fundamental async shift: 47% coherence improvement
# - All async methods: generate_reply_async, observe_async, etc.
# - asyncio.Lock for thread-safe field updates
aiofiles>=23.0.0

# Optional: OpenAI for GPT observer tests
# openai>=1.0.0
# httpx>=0.25.0
